<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
            http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context
            http://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/mvc
            http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 如果 Controller / Interceptor 里也需要 @Value，则这里也加载一次 -->
    <context:property-placeholder
            location="classpath:db.properties,classpath:ai-config.properties"
            file-encoding="UTF-8"/>

    <!-- 扫描 Controller、异常处理、拦截器 -->
    <context:component-scan base-package="com.study.ai.controller,com.study.ai.exception,com.study.ai.interceptor"/>

    <!-- 开启注解驱动 -->
    <mvc:annotation-driven/>

    <!-- 静态资源放行 -->
    <mvc:default-servlet-handler/>
    <mvc:resources mapping="/static/**" location="/static/"/>
    <mvc:resources mapping="/uploads/**" location="/uploads/"/>

    <!-- JSP 视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 文件上传解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="104857600"/>
        <property name="maxUploadSizePerFile" value="52428800"/>
        <property name="maxInMemorySize" value="4096"/>
    </bean>

    <!--
        拦截器示例：
        前提：你的拦截器类上加 @Component，例如：
        @Component
        public class LoginInterceptor implements HandlerInterceptor {}
    -->
    <!--
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/post/**"/>
            <mvc:mapping path="/resource/**"/>
            <mvc:mapping path="/ai/**"/>
            <mvc:exclude-mapping path="/user/login"/>
            <mvc:exclude-mapping path="/user/register"/>
            <ref bean="loginInterceptor"/>
        </mvc:interceptor>

        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <ref bean="adminInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
    -->

</beans>