<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.ai.mapper.CommentMapper">

    <resultMap id="CommentMap" type="com.study.ai.pojo.entity.Comment">
        <id property="id" column="id"/>
        <result property="resourceId" column="resource_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="nickname" column="nickname"/>
    </resultMap>

    <insert id="insert" parameterType="com.study.ai.pojo.entity.Comment">
        INSERT INTO comment (resource_id, user_id, content, create_time)
        VALUES (#{resourceId}, #{userId}, #{content}, NOW())
    </insert>

    <select id="findByResourceId" resultMap="CommentMap">
        SELECT c.*, u.nickname
        FROM comment c
                 LEFT JOIN user u ON c.user_id = u.id
        WHERE c.resource_id = #{resourceId}
        ORDER BY c.create_time DESC
    </select>

    <delete id="deleteByResourceId">
        DELETE FROM comment WHERE resource_id = #{resourceId}
    </delete>

</mapper>
